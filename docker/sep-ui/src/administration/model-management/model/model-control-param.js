import {ModelBase} from 'administration/model-management/model/model-base';

/**
 * Represents a model of a control param extending the {@link ModelBase} class.
 * A model control  are bound to {@link ModelField}.
 *
 * @author svelikov
 */
export class ModelControlParam extends ModelBase {

  constructor(id) {
    super(id);
  }

}