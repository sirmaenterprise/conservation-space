package com.sirmaenterprise.sep.roles.events;

import com.sirma.itt.seip.annotation.Documentation;
import com.sirma.itt.seip.event.EmfEvent;

/**
 * Event fired to notify for changes the in registered actions
 *
 * @author BBonev
 */
@Documentation("Event fired to notify for changes the in registered actions")
public class ActionDefinitionsChangedEvent implements EmfEvent {
	// nothing to provide
}
