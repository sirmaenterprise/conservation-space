PREFIX emf: <http://ittruse.ittbg.com/ontology/enterpriseManagementFramework#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
DELETE {
    emf:markedForDownload emf:isSearchable "true"^^xsd:boolean.
}
INSERT {
    emf:markedForDownload emf:isSearchable "false"^^xsd:boolean.
}
WHERE {
    
}