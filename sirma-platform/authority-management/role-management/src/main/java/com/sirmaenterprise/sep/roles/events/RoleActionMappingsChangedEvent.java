package com.sirmaenterprise.sep.roles.events;

import com.sirma.itt.seip.annotation.Documentation;
import com.sirma.itt.seip.event.EmfEvent;

/**
 * Event fired to notify for changes the in registered roles
 *
 * @author BBonev
 */
@Documentation("Event fired to notify for changes the in registered roles")
public class RoleActionMappingsChangedEvent implements EmfEvent {
	// nothing to add
}
