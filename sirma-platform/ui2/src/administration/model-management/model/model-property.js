import {ModelBase} from 'administration/model-management/model/model-base';

/**
 * Represents a model of a property extending the {@link ModelBase} class.
 * This model directly represents a semantic property.
 *
 * @author Svetlozar Iliev
 */
export class ModelProperty extends ModelBase {

  constructor(id) {
    super(id);
  }
}