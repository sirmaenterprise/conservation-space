import {ModelAttributeMetaData} from 'administration/model-management/meta/model-attribute-meta';

/**
 * Represents a specific header meta data extending from {@link ModelAttributeMetaData}
 *
 * @author svelikov
 */
export class ModelHeaderMetaData extends ModelAttributeMetaData {

  constructor(id) {
    super(id);
  }
}
