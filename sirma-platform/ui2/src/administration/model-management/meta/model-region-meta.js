import {ModelAttributeMetaData} from 'administration/model-management/meta/model-attribute-meta';

/**
 * Represents a specific region meta data extending from {@link ModelAttributeMetaData}
 *
 * @author Svetlozar Iliev
 */
export class ModelRegionMetaData extends ModelAttributeMetaData {

  constructor(id) {
    super(id);
  }
}