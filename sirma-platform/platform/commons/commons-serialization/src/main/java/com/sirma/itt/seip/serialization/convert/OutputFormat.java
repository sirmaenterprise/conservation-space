package com.sirma.itt.seip.serialization.convert;

/**
 * The possible output serialization formats
 *
 * @author BBonev
 */
public enum OutputFormat {

	/** The XML. */
	XML, /** The JSON format. This depends to Jettison library. */
	JSON
}